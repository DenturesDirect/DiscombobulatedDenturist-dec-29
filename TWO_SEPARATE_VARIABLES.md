# ✅ Two Separate Variables Needed

## You Need TWO Variables (Not One!)

### Variable 1: SUPABASE_URL
- **Name:** `SUPABASE_URL`
- **Value:** `https://qhexbhorylsvlpjkchkg.supabase.co`
- **That's it - just the URL!**

### Variable 2: SUPABASE_SERVICE_ROLE_KEY
- **Name:** `SUPABASE_SERVICE_ROLE_KEY`
- **Value:** `sb_secret_...` (your secret key from Supabase)
- **That's it - just the key!**

---

## How to Add/Edit Each One

### For SUPABASE_URL:
1. **Find `SUPABASE_URL` in the list**
2. **Click three dots (⋯) → Edit**
3. **In Value field, enter ONLY:**
   ```
   https://qhexbhorylsvlpjkchkg.supabase.co
   ```
4. **Save**

### For SUPABASE_SERVICE_ROLE_KEY:
1. **Find `SUPABASE_SERVICE_ROLE_KEY` in the list**
2. **Click three dots (⋯) → Edit**
3. **In Value field, enter ONLY:**
   ```
   sb_secret_... (your actual key)
   ```
4. **Save**

---

## They Are Separate!

- **SUPABASE_URL** = Just the URL
- **SUPABASE_SERVICE_ROLE_KEY** = Just the key

**Don't put them in the same field!**

---

**Edit each variable separately and add its own value!**
